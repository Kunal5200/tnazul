import { Poppins } from "next/font/google";

export const poppins = Poppins({
  weight: "500",
  subsets: ["latin"],
});

export const poppins700 = Poppins({
  weight: "700",
  subsets: ["latin"],
});
